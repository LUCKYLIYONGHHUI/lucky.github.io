<%- partial('_partial/page-header') %>
<main class="layout">
  <section>
    <% site.posts.each(function(post){ %>
      <article>
        <a href="<%= url_for(post.path) %>"><%= post.title %></a>
        <span><%= post.date.format('YYYY-MM-DD') %></span>
      </article>
    <% }) %>
  </section>
</main>